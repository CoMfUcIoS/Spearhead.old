import { assert } from 'chai';
import framework from '../';

const _api   = framework.initialize().api;

describe('api', function() {
  describe('prepareQuery function', function() {
    it('Template test check', function() {

      assert.equal(1, 1);
    });
  });
});
