import { assert } from 'chai';
import framework from '../../../';

const $sim   = framework.initialize().$sim;

describe('$sim', function() {
  describe('watch function', function() {
    it('call it', function() {

      assert.equal(true, true);
    });
  });
});
